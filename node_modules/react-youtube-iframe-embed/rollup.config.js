import pkg from './package.json';
import makeRollupConfig from '../../make-rollup-config';

const env = process.env.NODE_ENV;
const isProd = env === 'production';

export default makeRollupConfig({
  pkg,
  input: './index.js'
});
